import styled from "styled-components";
import tw from "twin.macro";

export const StyleMenu = styled.div`
  ${tw`flex`}
`;

export const StyleMenuList = styled.ul`
  ${tw`flex flex-row w-[100%]`}
`;

export const StyleMenuItem = styled.li`
  ${tw``}
`;
